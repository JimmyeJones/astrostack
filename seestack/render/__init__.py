"""Headless rendering helpers (debayer, autostretch, thumbnails).

This package is deliberately free of any GUI (PySide6) dependency so the web
server and worker processes can import it without pulling in Qt.
"""
