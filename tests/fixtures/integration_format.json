{
  "_comment": [
    "One table of integration-time cases, read by BOTH suites, so the app has one",
    "vocabulary for 'how much light went into this picture'.",
    "",
    "Two implementations format it: `formatIntegration` in `frontend/src/format.ts`",
    "(every screen) and `format_duration` in `seestack/sharecard.py` (the copyable",
    "caption, the baked nameplate, the montage strip, the recap poster, the",
    "before/after labels). They used to disagree — the SPA printed '3.2 h' where a",
    "caption for the very same run printed '3h 12m' — which is exactly what the",
    "comment on `Library.tsx`'s `expo` helper forbids: a beginner cannot tell that",
    "two spellings are one number.",
    "",
    "Readers: tests/test_sharecard.py and frontend/src/format.test.ts. A change to",
    "either formatter that isn't a change to the other now reddens a suite.",
    "",
    "Positive durations only, on purpose: the nothing-to-say case is the one",
    "deliberate difference. A stat tile renders an em dash; a caption drops the",
    "clause entirely rather than posting a placeholder. Each side pins its own.",
    "",
    "Each case is [seconds, expected]."
  ],
  "cases": [
    [1, "1 s"],
    [30, "30 s"],
    [59, "59 s"],
    [59.5, "1 min"],
    [60, "1 min"],
    [90, "2 min"],
    [150, "3 min"],
    [1800, "30 min"],
    [3569, "59 min"],
    [3571, "1.0 h"],
    [3600, "1.0 h"],
    [5400, "1.5 h"],
    [11520, "3.2 h"],
    [15150, "4.2 h"],
    [35999, "10.0 h"],
    [36000, "10 h"],
    [180000, "50 h"],
    [3600000, "1000 h"]
  ]
}
